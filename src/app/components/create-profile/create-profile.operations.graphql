mutation createProfile($user: UserInput!) {
  createProfile(user: $user) {
    id
    firstName
    lastName
    age
    phone
    picture {
      fileName
    }
    address {
      streetAddress
      aptSuit
      city
      zipCode
      stateAbbr
    }
  }
}
