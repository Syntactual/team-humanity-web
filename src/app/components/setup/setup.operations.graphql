mutation createProfile($user: UserInput!) {
  createProfile(user: $user) {
    id
    firstName
    lastName
    age
    about
    profilePicture {
      fileName
    }
    address {
      streetAddress
      aptSuit
      state
      zipCode
      stateAbbr
    }
  }
}
